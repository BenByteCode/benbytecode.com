version: 1
frontend:
  # 1. Build Phases
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  
  # 2. Artifacts (Output)
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

